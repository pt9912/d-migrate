# Dependencies
node_modules/
vendor/

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build outputs
dist/
build/
*.log
.gradle/

# Test coverage
coverage/
!docs/coverage/
!docs/coverage/excludes-ledger.md
*.coverage

# Temporary files
tmp/
temp/

# Release preflight / download artifacts
release/
release-assets/

# d-migrate runtime data (credentials, checkpoints, cache, audit)
.d-migrate/
# Codex
.codex

# Claude — lokale Einstellungen bleiben draussen, geteilte Skills gehoeren ins Repo.
# `.claude/*` statt `.claude/`: mit Schrägstrich steigt Git nicht ins Verzeichnis ab,
# und jede Negation darin bliebe wirkungslos.
.claude/*
!.claude/skills/

#
.kotlin/

#
coverage.json
scripts/__pycache__/

# Cached semgrep rules (LGPL-2.1 + Commons Clause upstream → not vendored;
# fetched + pinned + SHA256-verified by scripts/fetch-semgrep-rules.sh). README stays.
config/semgrep/*.yml

# Trivy-Vuln-DB (make image-scan) — Cache, kein Artefakt
.cache/
