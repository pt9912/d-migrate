schema_format: "1.0"
name: "E006 Test"
version: "1.0.0"

tables:
  items:
    columns:
      id:
        type: identifier
        auto_increment: true
      status:
        type: enum
    primary_key: [id]
