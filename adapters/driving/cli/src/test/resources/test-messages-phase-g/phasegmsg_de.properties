# Phase G R4 test bundle: DE overrides only the shared key.
# root.only.key is intentionally MISSING here so the test verifies
# the ResourceBundle parent-chain fallback to messages.properties.
shared.key=Geteilt Deutsch
