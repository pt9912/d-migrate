schema_format: "1.0"
name: "Test Schema B"
version: "2.0.0"

tables:
  users:
    columns:
      id:
        type: identifier
        auto_increment: true
      name:
        type: text
        max_length: 200
        required: true
      email:
        type: email
        required: true
    primary_key: [id]
