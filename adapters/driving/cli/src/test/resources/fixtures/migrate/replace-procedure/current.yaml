schema_format: "1.0"
name: "TestApp"
version: "1.0.0"

procedures:
  audit_call:
    parameters:
      - name: id_in
        type: integer
    language: plpgsql
    body: |
      BEGIN
        CALL audit_log(id_in);
      END
