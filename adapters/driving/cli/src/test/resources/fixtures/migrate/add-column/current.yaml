schema_format: "1.0"
name: "TestApp"
version: "1.0.0"

tables:
  users:
    columns:
      id:
        type: integer
        required: true
      email:
        type: text
        max_length: 254
        required: true
    primary_key: [id]
