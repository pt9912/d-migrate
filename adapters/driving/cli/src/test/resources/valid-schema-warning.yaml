schema_format: "1.0"
name: "Warning Schema"
version: "1.0.0"

tables:
  products:
    columns:
      id:
        type: identifier
        auto_increment: true
      price:
        type: float
    primary_key: [id]
