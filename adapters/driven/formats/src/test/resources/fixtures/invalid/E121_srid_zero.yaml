schema_format: "1.0"
name: "SRID Zero"
version: "1.0.0"

tables:
  places:
    columns:
      id:
        type: identifier
        auto_increment: true
      location:
        type: geometry
        geometry_type: point
        srid: 0
    primary_key: [id]
