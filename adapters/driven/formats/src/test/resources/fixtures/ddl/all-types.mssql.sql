-- Generated by d-migrate 1.0.3
-- Source: neutral schema v1.0.0 "All Types"
-- Target: mssql

-- [W137] JSON column 'col_json' is rendered as NVARCHAR(MAX): SQL Server has no native JSON column type; a reverse read yields plain text.
-- Hint: Add CHECK (ISJSON([col_json]) = 1) if the content must stay valid JSON.
-- [W137] Array column 'col_array' is rendered as NVARCHAR(MAX): SQL Server has no native Array column type; a reverse read yields plain text.
-- Hint: Add CHECK (ISJSON([col_array]) = 1) if the content must stay valid JSON.
CREATE TABLE [type_test] (
    [col_identifier] INT IDENTITY(1,1) NOT NULL,
    [col_text] NVARCHAR(255),
    [col_char] NCHAR(2),
    [col_integer] INT,
    [col_smallint] SMALLINT,
    [col_biginteger] BIGINT,
    [col_float_single] REAL,
    [col_float_double] FLOAT,
    [col_decimal] DECIMAL(10,2),
    [col_boolean] BIT,
    [col_datetime] DATETIMEOFFSET,
    [col_date] DATE,
    [col_time] TIME,
    [col_uuid] UNIQUEIDENTIFIER,
    [col_json] NVARCHAR(MAX),
    [col_xml] XML,
    [col_binary] VARBINARY(MAX),
    [col_email] NVARCHAR(254),
    [col_enum] NVARCHAR(1) CONSTRAINT [ck_type_test_col_enum] CHECK ([col_enum] IN (N'a', N'b', N'c')),
    [col_array] NVARCHAR(MAX),
    CONSTRAINT [pk_type_test] PRIMARY KEY ([col_identifier])
);
