# Trigger Watches und offene Vorabklärungen

Dieser Ordner sammelt Trigger, offene Folgearbeiten und Vorabklärungen,
die noch **keinen konkreten Scope** tragen. Sobald ein Scope formuliert
ist (Ziel + grobe Arbeitspakete + Akzeptanzkriterien), wandert der
Eintrag nach `../next/`.

Lebenszyklus und Verzeichnisstruktur sind in
[`ADR 0004`](../../adr/0004-documentation-and-planning-structure.md)
festgehalten.

## Konvention für Einträge

- Sprechender lowercase-kebab-Dateiname (z. B.
  `test-database-candidates.md`,
  `d-browser-integration-coupling-assessment.md`).
- Jeder Eintrag beschreibt im Kopf:
  - **Status**: `Draft` / `Vorschlag` / `Sammlung` / `Vorabklärung`
  - **Trigger**: was hat den Eintrag motiviert?
  - **Aktivierungsbedingung**: was muss passieren, damit er nach
    `../next/` wandert?
- Worked Examples und reine Referenz-/Sammlungs-Dokumente
  (z. B. `test-database-candidates.md`) dürfen hier liegen, auch
  wenn sie nie nach `next/` migrieren — sie sind dauerhaft
  "trigger-/referenz-haft" ohne Slice-Charakter.

## Wann **nicht** hierher

- Plan mit ausgearbeitetem Scope und Phasen → `../next/`.
- Aktiv bearbeiteter Slice mit Implementierungs-Commits →
  `../in-progress/`.
- Geliefert und geschlossen → `../done/`.
- Verworfen / vollständig überholt → `docs/archive/` (existiert
  bei Bedarf, siehe [ADR-0004](../../adr/0004-documentation-and-planning-structure.md)).

## Bestand

| Datei | Typ | Gegenstand |
| ----- | --- | ---------- |
| [`beispiel-stored-procedure-migration.md`](beispiel-stored-procedure-migration.md) | Worked Example | Beispiel fuer KI-gestuetzte Stored-Procedure-Migration von PostgreSQL nach MySQL. |
| [`cli-unimplemented-commands.md`](cli-unimplemented-commands.md) | Sammlung/Tracker | In `cli-spec.md` (Zielbild) beschriebene, aber noch nicht in der CLI registrierte Befehle (`transform procedure`, `generate procedure`, `data seed`, `validate data/procedure`, `config …`) + offene Trigger-Erweiterungen — Spec ↔ Requirement ↔ Milestone. |
| [`config-show-full-source-merge.md`](config-show-full-source-merge.md) | Vorabklärung / Trigger Watch | Restfläche aus `config show` Phase 1: der **vollständige** Multi-Source-Provenienz-Merge (Defaults + Datei + ENV + Flags mit Herkunft je Feld, `${VAR}`-Auflösung) statt nur der effektiv aufgelösten Konfig-Datei. Braucht eine Merge-/Provenienz-Schicht + vorgelagerte `cli-spec.md` §6.7-Präzisierung. Abgespalten aus dem graduierten Plan. |
| [`d-browser-integration-coupling-assessment.md`](d-browser-integration-coupling-assessment.md) | Vorabklaerung | Bewertung sichtbarer Kopplungen zwischen `d-browser` und `d-migrate` vor einem dedizierten `source-d-migrate`-Adapter. |
| [`mcp-server-spec-hygiene-residuals.md`](mcp-server-spec-hygiene-residuals.md) | Sammlung/Tracker | Rest-Hygiene in `spec/mcp-server.md` nach der Entphasung: 19 bare `§`-Referenzen (Stil + mehrdeutiges Ziel ImpPlan-B/ki-mcp) und eine möglicherweise veraltete `tools/call`-Verhaltensaussage (Vertrags-Genauigkeit). Nachtrag 2026-09-17 aus dem Compare-Slice: `artifact_upload_init` nimmt mehr Arten an, als die Spec nennt; `schema_list` filtert `jobId` gegen die Job-URI. |
| [`import-throughput-binary-copy.md`](import-throughput-binary-copy.md) | Draft (Trigger Watch) | COPY-Fast-Path auf weitere Typen ausweiten (Geometrie als **EWKB-Hex** statt `ST_GeomFromWKB`-SQL-Wrap; json/array/enum/interval/xml via COPY-Text/binär), die heute konservativ auf den INSERT-Rückfall fallen. Aktiviert bei geometrie-/json-lastigen Workloads mit COPY-Durchsatz-Ziel; nicht LF-blockierend. |
| [`partition-child-local-fk-transparency.md`](partition-child-local-fk-transparency.md) | Vorschlag (Draft) | Kind-lokale FKs auf PG-Partitionen (z. B. Pagila-`payment`-Kinder) beim Reverse erfassen, damit sie PG→PG round-trippen und PG→MySQL sichtbar via E065 verworfen werden statt still wegzufallen. Aus Carve-Out (`carveout.md` Abschnitt 9); braucht FK-Feld auf `PartitionDefinition` + ADR 0019/0020-Ergänzung. |
| [`partition-list-default-transfer-preflight.md`](partition-list-default-transfer-preflight.md) | Vorschlag (Draft) | Transfer-Zeit-Preflight, der die in eine PG-LIST-`DEFAULT`-Partition fallenden Zeilen zählt (kein MySQL-Pendant → Datenverlust), nach dem `CheckPreflight`-Muster (planner→runner→renderer→report). Ergänzt die bestehende statische Generate-Note E063. Aus Carve-Out (`carveout.md` Abschnitt 9). |
| [`mutation-testing-pit.md`](mutation-testing-pit.md) | Vorschlag (Draft) | PIT-Mutation-Testing für die JVM-Module als Test-Wirksamkeits-Signal über die Kover-Zeilen-Coverage hinaus. Trigger (stabile Coverage-Baseline + konsolidierte Excludes) erfüllt. Kernfrage = Kotlin-Reibung von Standard-PIT vs. Arcmutate; Start opt-in/Nightly auf `hexagon:core`. Aus Carve-Out (`carveout.md` Abschnitt 7). |
| [`pg-only-types-first-class-candidates.md`](pg-only-types-first-class-candidates.md) | Sammlung/Trigger-Watch | PG-only-Typen (`inet`, `cidr`, `tsquery`, Range/Multirange, `ltree`, `hstore`, `money`, `interval`, …), die heute zu `text` + R301 degradieren — Kandidaten für eine first-class-Modellierung (`geometry`-/`fulltext`-Muster), je Typ eine eigene Entscheidung bei belegtem Fidelity-Bedarf. Aus Carve-Out (`carveout.md` Abschnitt 8). |
| [`profiling-query-and-normalization.md`](profiling-query-and-normalization.md) | Vorschlag (Draft) | Nachruesten der in 0.7.5 bewusst zurueckgestellten Profiling-Flags `--query` und `--analyze-normalization` (Strukturanalyse, FD-Discovery, Normalisierungsvorschlaege) — ohne LLM-Schicht (Spec §10). |
| [`rest-grpc-artifact-ref-inheritance.md`](rest-grpc-artifact-ref-inheritance.md) | Vorschlag (Trigger Watch) | Auflage, dass die kuenftigen REST- (1.2.0) und gRPC- (1.1.8) Jobvertraege das opake `ServerResourceUri`-Artifact-Ref-Modell erben (MCP nutzt es bereits); aktiviert beim Bau der Services. |
| [`slice-adr-cross-class-direction-check.md`](slice-adr-cross-class-direction-check.md) | Tracker (wartet auf Tool-Feature) | Cross-Class-SDP-Richtung Vertrag › ADR › Slice mechanisieren, sobald d-check ein klassenübergreifendes Richtungs-Feature (slice↔adr) bietet; muss normativ-vs-Kontext unterscheiden, sonst flaggt es die 19 legitimen `adr→plan`-Provenienz-Refs. v0.30.0 `direction: no-downward` ist nur klassenintern (Spec-Straten Vertrag › Technik › Sicht). |
| [`sqlite-reverse-identifier-64bit-narrowing.md`](sqlite-reverse-identifier-64bit-narrowing.md) | Vorabklärung/Entscheidung | SQLite-Reverse mappt jede AUTOINCREMENT-PK (64-bit rowid) auf den 32-bit-`identifier`-Vertrag — Cross-Dialect-Transfer verengt still (m-trace-Befund 2026-07-02). Sofortmaßnahme geliefert (R202-Note + `generation` in schema.json); **Option 2 durch Reverse-Präferenzen-Slice abgelöst (2026-07-05)**; offen nur noch migrate-gegen-SQLite + Option 3. |
| [`sqlite-migrate-biginteger-identity-render-gap.md`](sqlite-migrate-biginteger-identity-render-gap.md) | Draft (Trigger Watch) | `migrate --execute` von authored `biginteger`+`identity` gegen ein SQLite-Ziel driftet aus **zwei** Ursachen: Diff/Rebuild-Renderpfad (`SqliteDiffSqlBuilders`) implementiert Identity nicht (AUTOINCREMENT still verloren) **und** die Reverse-Präferenz fädelt nicht in den Post-Compare-Re-Read. Abspaltung des Reverse-Präferenzen-Slices; P3. Nachtrag 2026-09-17: MySQL plant eine wirkungslose `MODIFY COLUMN` aus zwei Ursachen — die zielbewusste Naht wertet `legacy_serial_syntax` gegen MySQL (auch ohne Präferenz, bei jedem Soll ohne das Flag; eine Faltung dort berührt den Abdruck, ADR 0027) und der Ist-Stand wird ohne Präferenz gelesen (gemessen). |
| [`keychain-native-provider-module.md`](keychain-native-provider-module.md) | Vorschlag (Draft) / Trigger Watch (Nische) | Opt-in-Nischen-Modul `keychain-native` (JNA/DPAPI) auf demselben `KeychainBackend`-Port. `keychain:` deckt **alle drei OS native-frei** ab (inkl. Windows via PowerShell/`CredReadW`, [ADR 0040](../../adr/0040-keychain-credential-provider-backend-port.md)-Nachtrag) — das native Modul ist nur noch für Umgebungen sinnvoll, wo PowerShell nicht greift (Constrained Language Mode) oder echt-native bevorzugt wird. Ungebaut bis belegter Nischen-Bedarf. |
| [`jdbc-kopplung-der-ports-ist-aufnahme.md`](jdbc-kopplung-der-ports-ist-aufnahme.md) | Referenz/Ist-Aufnahme | Verifizierte Faktenlage zur JDBC-Kopplung der Ports-Schicht, als Zuarbeit zu [ADR 0037](../../adr/0037-database-agnostic-first-staffelung.md) („Database-Agnostic First", Umsetzung nach 1.0.0). **Ursache**: `DatabaseDriver` verlangt ohne Default `urlBuilder(): JdbcUrlBuilder`, `ddlGenerator()`, `tableLister()` — der Port abstrahiert nicht, er diktiert. **Symptome**: `TargetColumn.jdbcType`, `JdbcTypeHint.jdbcType`, `JdbcTypeCodes`. Heute belastet: `formats` (Nicht-JDBC-Adapter, muss trotzdem `java.sql.Types` sprechen). Kein eigenständiges Vorhaben; wandert nicht nach `next/`. |
| [`test-database-candidates.md`](test-database-candidates.md) | Referenzsammlung | Externe Beispieldatenbanken fuer Smoke-, Regression-, Streaming-, Resume- und Integrationsverifikation. |
| [`tool-comparison.md`](tool-comparison.md) | Referenzsammlung | Capability-Matrix d-migrate vs. Migrations-/Schema-/Bulk-Load-Tools (Ora2Pg, AWS SCT+DMS, Atlas, Liquibase/Flyway, pgloader, COPY, pg_bulkload), quellenbelegt + due-diligence-gehärtet. **#2 Head-to-Head erledigt + 2026-06-25 neu gemessen** (`make sample-db-tool-compare`: nach Schritt 0 + COPY ist d-migrate ~3,0× COPY (vorher ~4,6×) / ~1,7× pgloader; Import ~86k→~174k rows/s, Abstand zur Decke ~5,4×→~2,8×; diagnostisch). **Kein** Audit-Zahlen-Ziel. |
| [`audit-readiness-package.md`](audit-readiness-package.md) | Folgearbeit / Tracker | Auditor-Scope-/Onboarding-Doc und Dependency-Inventar als benannte Folgearbeit zu [ADR 0039](../../adr/0039-externer-security-audit-kein-1.0.0-gate.md). Kern-Deliverables geliefert 2026-07-19; **kein 1.0.0-Gate** — die Roadmap-Zeile schliesst nur ein tatsaechlicher Dritt-Bericht. |
| [`dcheck-versions-module.md`](dcheck-versions-module.md) | Vorschlag / Trigger Watch | d-check-Modul `versions` gegen [`version.md`](../../../version.md) verdrahten, damit der wandernde Anker maschinell geprueft wird statt nur konventionell. |
| [`spatial-profile-e052-ganze-tabelle.md`](spatial-profile-e052-ganze-tabelle.md) | Vorabklärung / Entscheidung offen | Vertragsfrage aus der Konsumentenmessung (Slice-Posten A6): `E052` verwirft auf SpatiaLite die **ganze** Tabelle, wenn eine Geometriespalte `NOT NULL` traegt. Der Ausgang ist laut und an drei Spec-Stellen festgeschrieben — zu entscheiden ist, ob Tabellenblockade die richtige Antwort auf ein Spaltenproblem bleibt. |
| [`json-jsonb-zweite-json-art.md`](json-jsonb-zweite-json-art.md) | Vorabklärung / Entscheidung offen | Modellfrage aus der Konsumentenmessung (Slice-Posten B3): PG `json` und `jsonb` kollabieren auf einen neutralen `Json` (`spec/neutral-model-spec.md:148` normiert das fuer die Renderrichtung). Ob das Modell die beiden trennen soll, ist eine Erweiterung nach dem `geometry`-/`fulltext`-Muster — die PG-only-Kandidatenliste schliesst `json`/`jsonb` ausdruecklich aus. |
| [`action-required-notiz-allein.md`](action-required-notiz-allein.md) | Vorabklärung / Trigger-Watch | Objekte, die nur als `action_required`-Notiz ausfallen und **nicht** in `skipped_objects` gezaehlt werden: `RawSqlExpressionPortability.indexRefusal` (alle fuenf Dialekte) und der Zusammengesetzte Typ E054 (Oracle/MSSQL/MySQL notiz-allein, SQLite zaehlt). Aus der DoD-Pruefung des Konsumentenbefunde-Slices. |
| [`make-target-authority-table.md`](make-target-authority-table.md) | Vorschlag / Trigger Watch | Autoritaets-Tabelle fuer `make`-Targets als Voraussetzung fuer das d-check-Modul `targets` (`DC-FA-TGT-001`): kein halluziniertes und kein undokumentiertes Gate. |
| [`security-gates-not-in-ci.md`](security-gates-not-in-ci.md) | Befund mit Erhebung / **beide Anliegen erledigt**, offen bleibt die Fussnote zu [ADR 0039](../../adr/0039-externer-security-audit-kein-1.0.0-gate.md) | semgrep und a-check stehen in `make gates`, laufen aber in KEINEM Workflow; zeitgesteuert laeuft nichts Sicherheitsrelevantes. `dependency-submission` war fuenf Wochen defekt (behoben `66a27d99`) — ADR 0039 nennt alle drei als 1.0.0-Interimslatte. **Erledigt**: semgrep + a-check als Job `security-gates` in build.yml (2026-08-15); zeitgesteuerter Teil als **Trivy**-Nightly `image-scan.yml` gegen die publizierten `:latest`/`:native` (2026-08-16, Eigner-Entscheidung). Trivy fand sofort etwas, das Dependabot strukturell NICHT sieht: geshadetes Jackson 2.21.3 in `parquet-jackson`. |
| [`dependency-cve-exposure-shipped-artifact.md`](dependency-cve-exposure-shipped-artifact.md) | Befund mit Erhebung / Trigger Watch | **90 real verwundbare Abhaengigkeitsversionen im publizierten `1.0.0`-Image** (1 kritisch, 43 hoch), gemessen an `/opt/d-migrate/lib` statt am Graphen — von 110 Alerts betreffen 20 nichts Ausgeliefertes. 33 der 44 kritischen/hohen sind **Netty 4.1.100.Final** aus dem Hadoop-Baum unter `formats-parquet` (38 von 140 MiB, inkl. YARN- und HDFS-Client). **ABGESCHLOSSEN 2026-08-16: 90 → 0** real verwundbare Versionen im Auslieferungsartefakt (vorher 1 kritisch, 43 hoch); Artefakt 240 → 177 Jars, 143 → 106 MB. Eigene Pins gehoben (PostgreSQL 42.7.12, Jackson 2.21.5, Logback 1.5.34), `io.netty`/ZooKeeper/BouncyCastle/YARN/HDFS aus den Hadoop-Deps ausgeschlossen, `commons-beanutils`/`aircompressor` per Constraint gehoben. Funktional ueber die ausgelieferte CLI verifiziert. Guava musste NICHT gehoben werden: es kam ueber `guice`/`guice-servlet` (YARN-Webapps, ungenutzt) und ueber `hadoop-common`, das fuer sich selbst `hadoop-shaded-guava` mitbringt — beide Pfade per `exclude` entfernt. Weg 3 (Hadoop ganz raus) durchgemessen und verworfen, siehe [ADR 0046](../../adr/0046-hadoop-bleibt-im-parquet-adapter.md). |
| [`column-generation-diff-unmapped.md`](column-generation-diff-unmapped.md) | Restflaeche | `ColumnDiff.generation` bildet laengst auf `DiffOperation.AlterColumnGeneration` ab; gebaut sind alle Uebergaenge, die die fuenf Server in place koennen — zuletzt (2026-09-13) PostgreSQLs `ADD GENERATED … AS IDENTITY` samt Sequenz-Nachzug. Offen: der Kind-Wechsel auf MySQL/Oracle/SQL Server (Spaltentausch mit Datenkopie) und das Verhaeltnis zu `AlterColumnType`. |
| [`oci-image-multiarch-jvm.md`](oci-image-multiarch-jvm.md) | Vorschlag / Trigger Watch | Das publizierte JVM-Image ist Single-Platform `linux/amd64` (verifiziert am `1.0.0-RC2`-Tag); Multi-Arch `linux/amd64` + `linux/arm64` steht aus. |
| [`oci-image-multiarch-native.md`](oci-image-multiarch-native.md) | Vorschlag / Trigger Watch | Dasselbe fuer `:X.Y.Z-native` — auf ARM64 laeuft es emuliert, was den Sinn eines nativen Images untergraebt. |
| [`warn-code-ledger-completeness.md`](warn-code-ledger-completeness.md) | Tracker | 8 emittierte W-Codes (W100/102/103/111/127/128/200/201) fehlen im YAML-Ledger; `CodeLedgerValidationTest` erzwingt keine W-Code-Vollständigkeit gegen den Source. AP1 Backfill + AP2 Gate-Härtung (symmetrisch zur E-Code-Vollständigkeit). Aufgedeckt beim W132-Fulltext-Code. |
| [`kotlin-gradle-plugin-cve-beta.md`](kotlin-gradle-plugin-cve-beta.md) | Trigger-Watch | CVE-2026-53914 verlangt `2.4.20-Beta1`; ein Beta in der Build-Toolchain ist eine Abwägung. Keine Produktfläche (wird nie ausgeliefert), fällt aber durch beide Raster — Graph-Filter und Versions-Updates. Aktiv bei stabilem 2.4.20. |
| [`json-schema-validator-3-jackson3.md`](json-schema-validator-3-jackson3.md) | Tracker / Vorabklaerung | `com.networknt:json-schema-validator` 3.x zieht Jackson 3 nach und bricht die Kompilierung in zwei Modulen (Dependabot-PR #32 geschlossen, Befund festgehalten). Trigger-Watch, bis das Projekt aus anderem Grund auf Jackson 3 geht. |
| [`check-ausdruck-analyse-per-parser.md`](check-ausdruck-analyse-per-parser.md) | Befund / Vorabklärung | `E012` entscheidet ohne Parser, welche Wörter eines CHECK-Ausdrucks Spaltenbezüge sind. Die erste Schätzung lehnte jeden Funktionsaufruf und jeden Cast ab (an einem externen Anwender aufgefallen); die zweite erkennt an der Stellung im Ausdruck und ist gebaut. Zielform ist der Port + Parser-Adapter aus `spec/jsqlparser-adapter.md`; die drei Vorfragen sind **gemessen** (JSQLParser 5.3 liest alle zwölf bekannten Fragmente inkl. `::`/`ARRAY[]`/`~` und liefert dieselben Spalten; Lizenz doppelt LGPL/Apache mit `jmh-core` als auszuschliessender Laufzeit-Abhaengigkeit; keine Reflection-Indikatoren im Jar, Native-Nachweis bleibt ein CI-Bau). Eigner-Entscheidung, weil eine neue Laufzeit-Abhängigkeit im Auslieferungsartefakt eine Zusage ist. |
| [`schema-refresh-materialized-view.md`](schema-refresh-materialized-view.md) | Befund / Vorabklärung | Der MV-Vertrag im Bericht nennt einen Refresh-Schritt, den es als Befehl nicht gibt (`BLOCKED_SCHEMA_REFRESH_UNSUPPORTED`). `schema refresh materialized-view` steht in `spec/cli-spec.md` nur als geblockte Absicht in einer Tabellenzeile, nicht als Befehl — ihn zu bauen hiesse, ihn zuerst zu spezifizieren. Eigner-Frage: gehoert Auffrischen (Daten, nicht Schema-Form) zur `schema`-Gruppe? |
| [`mssql-testimage-2025-cu1-startet-nicht.md`](mssql-testimage-2025-cu1-startet-nicht.md) | Befund (Test-Infrastruktur) | Das in `examples/sample-db` gepinnte MSSQL-Image `2025-CU1-ubuntu-24.04` stirbt auf dem Messhost mit einem paldumper-Abbild, auch ohne Compose (`2025-latest` und `2022-latest` laufen). Trifft den MSSQL-Leg des sample-db-Harness, der als best-effort-Gate still rot sein darf. Kein Produktdefekt. Nachtrag 2026-09-17: das Volltext-Testimage pinnt das Paket `mssql-server` nicht — Kandidat für den FTS-Ausreißer in CI an `e3116c34c` (ungemessen). |
| [`preserve-window-serialized-opt-in.md`](preserve-window-serialized-opt-in.md) | Vorabklaerung (wartet auf Messung) | Oracles Preserve-Fenster ist `SERIALIZED`, nicht `ATOMIC`. Der Slice davor macht das als Warnung sichtbar; ob daraus ein Blocker mit Opt-in werden soll, haengt am gemessenen Fehlerfall. |
| [`compare-serververgebene-namen-messauftrag.md`](compare-serververgebene-namen-messauftrag.md) | Vorabklärung (Messauftrag) | Eigner-Entscheidung F4 aus dem Compare-Slice ([ADR 0057](../../adr/0057-schema-compare-eine-semantik-herkunft-kein-unterschied.md)): `schema compare` nimmt nur den Identity-Sequenznamen aus. Ob `namesFullTextIndexes`, `namesPartitions` und `namesSingleColumnConstraints` dort Fehlalarme erzeugen, ist erst an je einem Paar zweier Reverses zu messen. |
| [`reverse-praeferenzen-oberflaechen-restflaechen.md`](reverse-praeferenzen-oberflaechen-restflaechen.md) | Sammlung | Drei Restflächen der Lese-Präferenzen aus dem Compare-Slice: kein Tool-Argument pro MCP-Aufruf (Tool-Schema, Idempotenz), `PreferenceSource` kennt keine Oberfläche (`R202` nennt in der CLI ohne Flag nur den Konfigurationsschlüssel, spec-konform), `data transfer` gibt keine Reader-Notes aus („Nicht stumm" gilt dort nicht). |
| [`compare-different-ohne-sichtbaren-fund.md`](compare-different-ohne-sichtbaren-fund.md) | Befund | `schema compare` sagt `DIFFERENT` ohne sichtbaren Grund: eine Partitionierungs-Änderung hat keinen MCP-Fund (in `ObjectDiffFieldsCompletenessTest` als Lücke gepinnt), und zwei handgeschriebene Schemata, die sich nur in Name oder Version unterscheiden, melden „0 change(s)". Aus dem Compare-Slice. |
| [`mcp-schema-compare-validiert-nicht.md`](mcp-schema-compare-validiert-nicht.md) | Vorabklärung / Eigner-Frage | Die CLI endet bei einem ungültigen Schema (`E012`) mit Exit 3, `schema_compare` und `schema_compare_start` vergleichen trotzdem. Ob die Validierung zur einen Semantik von `schema compare` gehört, entscheidet der Eigner; in P11 des Compare-Slices bewusst ausgenommen. |
| [`compare-unbenannte-indizes-kanonische-zuordnung.md`](compare-unbenannte-indizes-kanonische-zuordnung.md) | Befund / Korrektur | Ein unbenannter Index wird über das **rohe** Prädikat zugeordnet (`TableIndexComparator.indexKey`); eine reine Schreibweise-Differenz erscheint als entfernt + hinzugefügt. Korrektur, keine Toleranz (Toleranzprofil, „Geprüft und nicht aufgenommen"); nur handgeschriebene Dateien betroffen. |
| [`reverse-umfang-cli-gegen-mcp.md`](reverse-umfang-cli-gegen-mcp.md) | Befund / Vorabklärung | `schema reverse` liest Sichten und Routinen nur mit `--include-*`, `schema_reverse_start` immer — Reverse-Dateien beider Wege unterscheiden sich im Umfang, und der Report nennt ihn nicht. Der PostGIS-in-`public`-Teil liegt im Reader-Slice (A3). |
| [`mcp-e2e-oracle-nicht-gefahren.md`](mcp-e2e-oracle-nicht-gefahren.md) | Befund (Test-Infrastruktur) | Oracle ist in Roundtrip und 5x5-Compare-Matrix von `examples/mcp-e2e` nie gefahren (ein fremder `mcp-e2e-oracle-1` im Compose-Projekt würde übernommen); die Oracle-Zellen sind nicht gepinnt. |
| [`ci-verdeckte-fehlschlaege.md`](ci-verdeckte-fehlschlaege.md) | Befund / Vorabklärung | Dreizehn Workflows (sechs Sample-DB-Cross-Smokes, sieben weitere) tragen job-weites `continue-on-error` — ein roter Smoke ist ein grüner Haken; `integration.yml` läuft ohne `--continue`, ein Ausreißer verdeckt deterministische Fehler (gemessen an `e3116c34c`). Dazu der erste CI-Lauf der Compare-Matrix als Beobachtungspunkt. |
| [`mcp-verdrahtungsluecken.md`](mcp-verdrahtungsluecken.md) | Befund | `mcp serve` verdrahtet `EmptyDiffStore`/`EmptyProfileStore` — `diff_list` findet das Compare-Artefakt nicht, obwohl `spec/mcp-server.md` den Index beschreibt; der `job_input`-Upload ist über das Eingabeschema von `artifact_upload_init` nicht erreichbar (`approvalKey`, `artifactKind` fehlen). Vorbestehend, aus den Reviews des Compare-Slices. |
| [`cli-usage-fehler-exit-1-statt-2.md`](cli-usage-fehler-exit-1-statt-2.md) | Befund | Clikt-Usage-Fehler (gemessen: ungültiger Auswahlwert der Lese-Flags) enden über `Main.kt` mit Exit 1; `spec/cli-spec.md` und der Troubleshooting-Leitfaden sagen 2. Vorbestehend und global, eine Stelle. |
| [`mssql-sicht-abhaengigkeiten-nicht-gelesen.md`](mssql-sicht-abhaengigkeiten-nicht-gelesen.md) | Befund / Vorabklärung | Der MSSQL-Reverse füllt `ViewDefinition.dependencies` nicht (`sys.sql_expression_dependencies` wird nur für Routinen und Trigger gelesen). Kein Fehlalarm in `schema compare`, aber der Migrate-Pfad liest das Feld: Beim Tabellen-Rename überspringt die Sicht-Neuprojektion eine Sicht ohne `dependencies` ohne Meldung (am Code gelesen, ungemessen). Zweite Hälfte von Punkt 5 des Compare-Umbrella. |
| [`compare-source-dialect-routinen-trigger.md`](compare-source-dialect-routinen-trigger.md) | Vorabklärung / Eigner-Frage | `schema compare` nimmt `sourceDialect` nur an Sichten aus dem Vergleich (AP2 des Compare-Umbrella); an Funktionen, Prozeduren und Triggern ist es weiter ein Fund. Spec-Grammatik und Toleranzprofil sagen je etwas anderes; kein Test legt den Routinenfall fest. |
| [`dcheck-pin-v0761-fail-closed.md`](dcheck-pin-v0761-fail-closed.md) | Befund / Vorabklärung (Gate-Infrastruktur) | Der gepinnte d-check `v0.74.1` meldet eine Kernänderung still, wenn ein Objekt nur in einem `loose-*`-Pack liegt; erst `v0.76.1` bricht dort ab (d-check CO-001). `make doc-immutable` prüft inzwischen gegen einen frischen Klon und ist nicht betroffen, andere git-lesende Aufrufe gegen das Arbeits-Repo schon. Wartet auf die Entscheidung, den Pin zu heben. |
