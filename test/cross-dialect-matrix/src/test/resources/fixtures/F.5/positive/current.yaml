schema_format: "1.0"
name: "matrix-F5-current"
version: "1.0.0"

tables:
  orders:
    columns:
      id:
        type: identifier
        auto_increment: true
      amount:
        type: integer
        required: true
    primary_key: [id]
